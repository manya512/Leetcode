class Solution(object):
    def isPerfectSquare(self, num):
        if num < 1:
            return False
        
        r = num
        while r * r > num:
            r = (r + num // r) // 2
            
        return r * r == num
            

        
